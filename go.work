go 1.25.6

// Local-dev workspace (gitignored). Lets gortex resolve a sibling
// checkout of gcx-go before it is tagged & published. Delete this
// file once `github.com/gortexhq/gcx-go v0.1.0` is live on the proxy
// — nothing else needs changing.
use (
	.
	../gcx-go
)
